[project]
name = "book-factory"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ruff>=0.15.13",
]

[tool.uv.workspace]
members = [
    "ruff",
]
